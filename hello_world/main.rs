// main is always the first code that runs
// parameters go inside ()
fn main() {
    // ! calls a rust macro instead of a normal function
    // most lines end with a semicolon
    println!("Hello World");
}

// compile code - rustc main.rs
// run the main file - ./main
// rust is ahead-of-time compiled, you can compile and give the executable to someone without rust